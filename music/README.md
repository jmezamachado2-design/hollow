# Música

Coloca pistas en esta carpeta (`.ogg`, `.wav` o `.mp3`). Usa `AudioManager.reproduce_musica("nombre")` o el export `music_track` en cada `Room`.

Ejemplos: `room_explore.ogg`, `room_battle.ogg`

Sin archivo, se usa un loop procedural de respaldo.
